{
  "name": "sniperhunterz-website",
  "version": "1.0.0",
  "private": true,
  "description": "Public dashboard for SniperHunterz display sync (no scanner)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "smoke": "node scripts/smoke.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.21.2"
  }
}
